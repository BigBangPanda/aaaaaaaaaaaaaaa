server:
  port: 8080
  context-path: /configserver

#server:
#  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BigBangPanda/BambooForest.git
#      profile: dev #对应业务名称的profile
#      label: master #当配置文件在git上时为分支名
#      name: foobar
#  application:
#    name: server #对应文件的业务名称