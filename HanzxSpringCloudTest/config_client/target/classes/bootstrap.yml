server:
  port:  8081

spring:
  cloud:
    config:
      uri: http://localhost:8080/configserver
      profile: dev #对应业务名称的profile
      label: master #当配置文件在git上时为分支名
  application:
#    name: client #对应文件的业务名称
    name: client